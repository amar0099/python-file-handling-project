# Python Mini Project – API → Filter → Save as CSV

This small project pulls data from an API, filters the required values, and saves the output into a CSV file using Python (Databricks Notebook).

## 🔁 Workflow

API → Fetch Data → Filter Data → Save as CSV

## 🐍 Technologies Used
- Python
- Databricks Notebook
- APIs (Requests)
- CSV / JSON file handling

